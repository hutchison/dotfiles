# Server Einstellungen:
set from = "martin.darmuentzel@uni-rostock.de"
set hostname = "uni-rostock.de"

# Wie wird gesendet?
set sendmail = "/usr/bin/msmtp -a uni"

# relevante Ordner:
set spoolfile = "+uni/Uni"
set mbox = "+uni/Archive"
set record = "+uni/Sent"
set postponed = "+uni/Drafts"
set trash = "+uni/Trash"

# für den 'A'-Hotkey lesen wir hotkeys.muttrc nochmals ein:
source ~/.mutt/hotkeys.muttrc
